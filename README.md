# Cypress AI — Homebrew tap

```sh
brew install --cask cypressai/tap/miito
```

`miito` — the Miito desktop app: terminals, a browser, and coding agents in one
window. macOS 14+ on Apple Silicon.

The build is not yet notarised, so read the caveats Homebrew prints after
install — the first launch needs the quarantine attribute cleared.
